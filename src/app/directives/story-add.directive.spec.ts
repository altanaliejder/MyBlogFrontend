import { StoryAddDirective } from './story-add.directive';

describe('StoryAddDirective', () => {
  it('should create an instance', () => {
    const directive = new StoryAddDirective();
    expect(directive).toBeTruthy();
  });
});
